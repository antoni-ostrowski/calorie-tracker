services:
  web:
    image: antost360/calorie-tracker:latest
    ports:
      - "80:3000"
    volumes:
      - ./data:/app/data
    environment:
      - OPENCODE_API_KEY=${OPENCODE_API_KEY}
    restart: unless-stopped
