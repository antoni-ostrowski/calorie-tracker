services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    environment:
      - DATABASE_URL=./data/app.db
      - OPENCODE_API_KEY=${OPENCODE_API_KEY}
      - OPENCODE_API_URL=${OPENCODE_API_URL:-https://api.opencode.ai/v1}
    restart: unless-stopped
